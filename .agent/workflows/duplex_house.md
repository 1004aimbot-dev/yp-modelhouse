---
description: Generates a small duplex house design based on pyeong size.
---

1. Run the generation pipeline
   // turbo
   ```bash
   python run_pipeline.py --pyeong 15 --name DuplexTinyHouse
   ```
